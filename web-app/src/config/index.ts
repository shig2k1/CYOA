const MAP_CHUNK_SIZE = 3
const MAP_HCHUNK_SIZE = Math.floor(MAP_CHUNK_SIZE / 2)
const MAP_GRID_SIZE = 5
const MAP_HGRID_SIZE = Math.floor(MAP_GRID_SIZE / 2)

export { MAP_CHUNK_SIZE, MAP_GRID_SIZE, MAP_HCHUNK_SIZE, MAP_HGRID_SIZE }
